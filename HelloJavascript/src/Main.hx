package ;

class Main {
	static function main() {
		// The below commented out line doesn't compile with error: Accessing this field requires a system platform (php,neko,cpp,etc.)
		//Sys.println("Hello World");
		trace("Hello World");
	}
}